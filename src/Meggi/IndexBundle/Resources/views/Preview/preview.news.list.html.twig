{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field%}
    <div>
        {% if object | itm_file_exists('preview') %}
            <img src="{{ object | itm_ipw_path('preview') | imagine_filter('crop_square') }}" />
        {% endif %}
    </div>
{% endblock %}
